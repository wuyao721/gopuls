;; -*- mode: lisp-interaction; coding: utf-8; -*-
;; This is a project configuration example for emacs

;; enable ede 
;(global-ede-mode 1)

;; enable sematic
;(semantic-mode 1)

;; add semanticdb-project-roots like these:
;(add-to-list 'semanticdb-project-roots "f:/project/goplus/")

;; add ede-cpp-root-project like these
;(ede-cpp-root-project "goplus"
;		      :name "go plus"
;		      :file "f:/project/goplus/premake4.lua"
;		      :include-path '("/include"
;				      )
;		      :system-include-path '("C:/devenv/include")
;		      :spp-table '(("WIN32" . "")
;				   )
;		      :spp-files '(
;				    ))
